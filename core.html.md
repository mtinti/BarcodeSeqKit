# BarcodeSeqKit Core


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Introduction

BarcodeSeqKit is a library designed for extracting and processing
barcoded sequences from next-generation sequencing data. This notebook
contains the core abstractions and base classes for the library.

## Barcode Configuration

First, let’s define the data structures for barcode configuration.

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L24"
target="_blank" style="float:right; font-size:smaller">source</a>

### OrientationType

>  OrientationType (value, names=None, module=None, qualname=None,
>                       type=None, start=1)

*Orientation types for barcode sequences.*

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L31"
target="_blank" style="float:right; font-size:smaller">source</a>

### BarcodeLocationType

>  BarcodeLocationType (value, names=None, module=None, qualname=None,
>                           type=None, start=1)

*Location types for barcodes.*

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L39"
target="_blank" style="float:right; font-size:smaller">source</a>

### BarcodeConfig

>  BarcodeConfig (sequence:str,
>                     location:__main__.BarcodeLocationType=<BarcodeLocationType
>                     .UNKNOWN: 'UNK'>, name:Optional[str]=None,
>                     description:Optional[str]=None)

*Configuration for a barcode sequence.*

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L70"
target="_blank" style="float:right; font-size:smaller">source</a>

### BarcodeMatch

>  BarcodeMatch (barcode:__main__.BarcodeConfig,
>                    orientation:__main__.OrientationType, position:int,
>                    sequence:str)

*Represents a match of a barcode in a sequence.*

## Extractor Base Classes

Next, let’s define the abstract base classes for the different
extractors.

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L83"
target="_blank" style="float:right; font-size:smaller">source</a>

### ExtractionStatistics

>  ExtractionStatistics (total_reads:int=0, total_barcode_matches:int=0,
>                            matches_by_barcode:Dict[str,int]=<factory>,
>                            matches_by_orientation:Dict[str,int]=<factory>,
>                            matches_by_category:Dict[str,int]=<factory>,
>                            no_barcode_count:int=0)

*Statistics collected during barcode extraction.*

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L173"
target="_blank" style="float:right; font-size:smaller">source</a>

### BarcodeExtractor

>  BarcodeExtractor (barcodes:List[__main__.BarcodeConfig],
>                        output_prefix:str, output_dir:Optional[str]=None,
>                        merge_orientations:bool=False,
>                        keep_unmatched:bool=True, verbose:bool=False,
>                        log_file:Optional[str]=None)

*Abstract base class for barcode extraction from sequencing data.*

Now let’s implement the specialized extractors for BAM and FASTQ files

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L278"
target="_blank" style="float:right; font-size:smaller">source</a>

### SequenceProcessor

>  SequenceProcessor ()

*Utility class for processing sequences and finding barcode matches.*

## Configuration Management

Let’s add classes for managing the library configuration:

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L320"
target="_blank" style="float:right; font-size:smaller">source</a>

### ExtractorConfig

>  ExtractorConfig (barcodes:List[__main__.BarcodeConfig],
>                       output_prefix:str, output_dir:str='.',
>                       merge_orientations:bool=False, keep_unmatched:bool=True,
>                       verbose:bool=False, log_file:Optional[str]=None,
>                       search_softclipped:Optional[bool]=False)

*Configuration for the barcode extractor.*

## Factory Pattern for Extractor Creation

Let’s implement a factory pattern to create the appropriate extractor
based on input file type:

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L404"
target="_blank" style="float:right; font-size:smaller">source</a>

### FileFormat

>  FileFormat (value, names=None, module=None, qualname=None, type=None,
>                  start=1)

*Supported file formats.*

## Factory Pattern for Extractor Creation

Let’s implement a factory pattern to create the appropriate extractor
based on input file type:

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L404"
target="_blank" style="float:right; font-size:smaller">source</a>

### FileFormat

>  FileFormat (value, names=None, module=None, qualname=None, type=None,
>                  start=1)

*Supported file formats.*

------------------------------------------------------------------------

<a
href="https://github.com/mtinti/BarcodeSeqKit/blob/main/BarcodeSeqKit/core.py#L422"
target="_blank" style="float:right; font-size:smaller">source</a>

### ExtractorFactory

>  ExtractorFactory ()

*Factory for creating barcode extractors based on file format.*

# Example Usage

Let’s demonstrate how to use the core components

``` python
# Example of creating a barcode configuration
barcode_5prime = BarcodeConfig(
    sequence="TCGCCAAGAGGC",
    location=BarcodeLocationType.FIVE_PRIME,
    name="5",
    description="5' barcode for phenotyping experiment"
)

barcode_3prime = BarcodeConfig(
    sequence="GCCTCGAATCGA",
    location=BarcodeLocationType.THREE_PRIME,
    name="3",
    description="3' barcode for phenotyping experiment"
)

# Create an extractor configuration
config = ExtractorConfig(
    barcodes=[barcode_5prime, barcode_3prime],
    output_prefix="example",
    output_dir="./output",
    merge_orientations=True
)

# Print the configuration
print(f"Barcode 5' sequence: {barcode_5prime.sequence}")
print(f"Barcode 5' reverse complement: {barcode_5prime.reverse_complement}")
print(f"Barcode 3' sequence: {barcode_3prime.sequence}")
print(f"Barcode 3' reverse complement: {barcode_3prime.reverse_complement}")
```

    Barcode 5' sequence: TCGCCAAGAGGC
    Barcode 5' reverse complement: GCCTCTTGGCGA
    Barcode 3' sequence: GCCTCGAATCGA
    Barcode 3' reverse complement: TCGATTCGAGGC

``` python
#barcode_5prime.sequence,barcode_5prime,barcode_3prime.sequence
```

``` python
# Example of finding barcode matches in a sequence
sequence = "AAAAAAATCGCCAAGAGGCAAAAAAA"
matches = SequenceProcessor.find_barcode_matches(sequence, [barcode_5prime, barcode_3prime])

for match in matches:
    print(f"Found {match}"=="Found 5 (FR) at position 7")
```

    True

``` python
# Example of finding barcode 3FR matches in a sequence
sequence = "AAAAAAAGCCTCGAATCGAAAAAAAA"
matches = SequenceProcessor.find_barcode_matches(sequence, [barcode_5prime, barcode_3prime])

for match in matches:
    print(f"Found {match}" == 'Found 3 (FR) at position 7')
```

    True

``` python
# Example of finding barcode 5 RC matches in a sequence
sequence = "AAAAAAAGCCTCTTGGCGAAAAAAAA"
matches = SequenceProcessor.find_barcode_matches(sequence, [barcode_5prime, barcode_3prime])

for match in matches:
    print(f"Found {match}"=='Found 5 (RC) at position 7')
```

    True

``` python
# Example of finding barcode 3 RC matches in a sequence
sequence = "AAAAAAATCGATTCGAGGCAAAAAAAA"
matches = SequenceProcessor.find_barcode_matches(sequence, [barcode_5prime, barcode_3prime])

for match in matches:
    print(f"Found {match}" == 'Found 3 (RC) at position 7')
```

    True

``` python
# Example of saving configuration to YAML
config.save_yaml("example_config.yaml")
print("Configuration saved to example_config.yaml")
```

    Configuration saved to example_config.yaml

## Conclusion

This notebook establishes the core abstractions for the BarcodeSeqKit
library. These classes provide the foundation for the specialized
extractors for different file formats, which will be implemented in
separate notebooks.
